---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: snowflakeresources.snowflake.my.domain
spec:
  group: snowflake.my.domain
  names:
    kind: SnowflakeResource
    listKind: SnowflakeResourceList
    plural: snowflakeresources
    singular: snowflakeresource
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SnowflakeResource is the Schema for the snowflakeresources API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SnowflakeResourceSpec defines the desired state of SnowflakeResource
            properties:
              objectConfig:
                description: ObjectConfig is a list of parameters for the Snowflake
                  object
                items:
                  description: Param defines the parameters for the snowflake object
                  properties:
                    key:
                      description: Key is the name of the parameter
                      type: string
                    value:
                      description: Value is the value of the parameter
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              objectType:
                description: ObjectType is the type of the Snowflake object
                type: string
            required:
            - objectConfig
            - objectType
            type: object
          status:
            description: SnowflakeResourceStatus defines the observed state of SnowflakeResource
            properties:
              name:
                type: string
              ready:
                type: boolean
              type:
                type: string
            required:
            - name
            - ready
            - type
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
